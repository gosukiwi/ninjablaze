define(function () {
  'use strict';

  return {
    sqrt: function (x) {
      return Math.sqrt(x);
    },

    square: function (x) {
      return x * x;
    },
  
    cube: function (x) {
      return x * x * x;
    },

    sin: function (x) {
      return Math.sin(x);
    }
  };
});
